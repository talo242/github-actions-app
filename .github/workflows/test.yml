name: Test

on:
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master

      - name: yarn install & test
        run: |
          yarn install
          yarn test
